node_modules
dist
.env
.env.local
*.log
.DS_Store

# Secretos de produccion: nunca al repo (ver .env.production).
.env.production

# Salida de `npm run mail:preview` (HTML de los correos para mirarlos)
.mail-preview/
